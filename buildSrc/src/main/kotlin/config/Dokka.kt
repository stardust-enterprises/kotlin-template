package config

object Dokka {
    /**
     * Custom external documentations, as an array of stringed URLs.
     *
     * Default: (empty).
     */
    val externalDocumentations: Array<String> = arrayOf(
        // "https://javadoc.io/doc/net.java.dev.jna/jna",
    )
}
